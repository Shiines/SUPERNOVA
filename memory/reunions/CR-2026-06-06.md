# CR — 2026-06-06 · Rebrand ATLAS → SUPERNOVA

> Capitalization report. Session: rename the lead piloting agent from `atlas` to `supernova`,
> scrub the last traces of the old brand name across the system.

---

## 1. Context

The studio brand had already been rebranded STUDIO ATLAS → SUPERNOVA during scaffold
(folder `SUPERNOVA/`, `08-supernova-brand/`, `supernova-rules.md`, `CLAUDE.md`).
The only `atlas` left was the **lead piloting agent** itself (vision / briefing / business strategy),
its `/atlas` command, and a few references. Decision (user): full rename + scrub the origin note.

## 2. What changed

| # | File | Change | Method |
|---|------|--------|--------|
| 1 | `02-agents/atlas.md` → `02-agents/supernova.md` | Agent renamed, title `# Agent: supernova` | `git mv` (history kept) |
| 2 | `.claude/commands/atlas.md` → `.claude/commands/supernova.md` | Command is now **`/supernova`** — Morning Briefing | `git mv` (history kept) |
| 3 | `02-agents/INDEX.md` | Router row now points to `supernova.md` | edit |
| 4 | `CLAUDE.md` | "Piloting" agents line + Session Loop now `/supernova` | edit |
| 5 | `05-clients/_template-client/01-brief.md` | `**Agent:** supernova` | edit |
| 6 | `00-system/GENESIS.md` | Origin line scrubbed of "SYSTÈME ATLAS" | edit |

## 3. Verification

- `grep -rni 'atlas'` across repo (excl. `.git`) → **0 hits**. Fully rebranded.
- Skill registry reloaded → `supernova: /supernova — Morning Briefing` (was `/atlas`).
- File renames staged as `RM` (rename) → git history preserved.

## 4. Impact / things to know

- ⚠️ **Muscle-memory change:** `/atlas` no longer exists → use **`/supernova`** for the morning briefing.
- No alias kept (user chose full rename). If `/atlas` is typed it will not resolve.
- The `supernova` agent file is still a **blank template** (role/triggers/tasks are placeholders) — fill it when defining the pilot persona.

## 5. Git state

Staged, **not committed** (push happens at `/fin`, co-author Mappa77):
```
RM .claude/commands/atlas.md -> .claude/commands/supernova.md
RM 02-agents/atlas.md       -> 02-agents/supernova.md
 M 00-system/GENESIS.md
 M 02-agents/INDEX.md
 M 05-clients/_template-client/01-brief.md
 M CLAUDE.md
```
Untracked (noise): `00-system/.DS_Store`, `00-system/workflow/.DS_Store`.

## 6. Next steps

- [ ] Define the `supernova` agent persona (file is still a template).
- [ ] Commit + push at `/fin`.
- [ ] Optional: add `.DS_Store` to `.gitignore` to stop macOS noise.
